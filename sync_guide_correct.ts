import { supabaseAdmin } from "@/integrations/supabase/client.server"; import { FALLBACK_VENDOR_GUIDE_SECTIONS } from "@/lib/vendor-guide.functions"; async function run() { const { error } = await supabaseAdmin.from("payment_gateway_settings").upsert({ provider: "vendor_guide_sections", enabled: true, config: FALLBACK_VENDOR_GUIDE_SECTIONS, updated_at: new Date().toISOString() }, { onConflict: "provider" }); console.log("Done", error); } run();
