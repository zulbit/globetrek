import { supabaseAdmin } from "@/integrations/supabase/client.server"; import { FALLBACK_VENDOR_GUIDE_SECTIONS } from "@/lib/vendor-guide.functions"; async function run() { for (const sec of FALLBACK_VENDOR_GUIDE_SECTIONS) { const { error } = await supabaseAdmin.from("payment_gateway_settings").upsert({ provider: "vendor_guide_store", config: { sections: FALLBACK_VENDOR_GUIDE_SECTIONS } }); console.log("Done", error); break; } } run();
